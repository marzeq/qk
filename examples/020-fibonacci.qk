let printf(s: *char, ...) = extern("printf")

let fib(n: i32): i32 = 
  if n <= 1 { n }
  else { fib(n - 1) + fib(n - 2) }

let main() = {
  for var i: i32 = 0; i < 40; i = i+1 {
    printf("fib(%d) = %d\n", i, fib(i))
  }
}
