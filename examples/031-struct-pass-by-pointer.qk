let printf(s: *char, ...) = extern("printf")

let Point = type struct {
  x: i32
  y: i32
}

let Point_print(p: *Point) = {
  printf("Point(%d, %d)\n", p.x, p.y)
}

let main() = {
  let p = Point{ x=10, y=20 }
  Point_print(&p)
}
