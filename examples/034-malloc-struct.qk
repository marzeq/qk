let printf(s: *char, ...) = extern("printf")
let malloc(size: u64): *void = extern("malloc")

struct Point = {
  x: i32
  y: i32
}

let main() = {
  var p = malloc(8) as *Point
  p.x = 10
  p.y = 20
  printf("%d\n", p.x)
}
